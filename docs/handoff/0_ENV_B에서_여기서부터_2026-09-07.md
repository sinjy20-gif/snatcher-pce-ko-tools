# 환경 B에서 여기서부터 — 2026-09-07

## 폴더

- `GAME_BUILD/0.6.0-rc1`: 이번에 가져갈 유일한 실기 빌드
- `snatcher_tool`: 최신 스튜디오 실행 파일·소스·번역 작업 데이터
- `HANDOFF_WEEKEND`: 9월 5일 토요일과 6일 일요일 인계서
- `project`: 환경 B 작업 폴더 `C:\snatcher`에 덮을 최신 프로젝트 작업본
- `SHA256_핵심파일.txt`: 핵심 실행 파일과 데이터 해시

## 바로 테스트

`GAME_BUILD/0.6.0-rc1`에서 다음 두 파일을 함께 사용한다.

1. BIOS: `Syscard3_galmuri_0.6.0-rc1.pce`
2. 게임: `Snatcher CD-ROMantic (Japan) [KO].cue`

Mesen은 전원부터 다시 켠다. 기존 세이브스테이트와 상태 변경 Lua는 쓰지 않는다.

## 작업 폴더 갱신

`project` 폴더의 내용은 `C:\snatcher` 기준 상대 경로다. 환경 B 컴퓨터의 기존
`C:\snatcher` 위에 덮어쓴다. 삭제 동기화가 아니라 복사 덮어쓰기만 한다.

그 뒤 `snatcher_tool` 폴더도 `C:\snatcher\snatcher_tool`에 덮어쓰면 최신
스튜디오와 번역 데이터가 맞는다.

상세 현황은 먼저
`HANDOFF_WEEKEND/SNATCHER_WEEKEND_2026-09-05_06_HANDOFF.md`를 읽는다.

