# 여기서부터 — 2026-09-11

## 먼저 읽을 것 (순서대로)

```
1. SNATCHER_TRACK5_PORTRAIT_PATTERN_COLLISION_2026-09-11.md
                                                ★트랙 5 초상화 한 칸 소실 해결
2. ENV_B_MERGE_2026-09-11.md                   09-10 환경 A 작업 병합 기록
3. SNATCHER_ENV_A_TO_ENV_B_2026-09-09.md        09-09 인계서
4. SNATCHER_GFX_NATIVE_REASSESS_2026-09-09.md   GFX 정본 (정정 절 포함)
5. SNATCHER_ANDROID_ADPCM_GATE_2026-09-08.md    ADPCM 조사 정본 (접은 건)
6. SNATCHER_ANDROID_BEETLE_PCE_BOOT_PROBE_PLAN_2026-09-09.md
                                                Android Beetle PCE 초기 부팅 실패 코어 프로브 계획
```

## 디스크는 안 실었다

환경 B의 `build/patch/0.6.2` 를 그대로 쓴다 — 그게 원래 환경 B에서 구워 환경 A로 온
판이다. 오늘 환경 A에서 한 것은 프로브·도구·문서·번역뿐이라 소스만 간다.

실패한 진단판 `0.6.3-diag-adstat-180c` 는 환경 A에서 이미 정리돼 없어서 안 실었다.
무엇을 왜 시도했고 왜 실패했는지는 인계서 §2 에 그대로 있다 — 되풀이하지 말 것.

## 작업 폴더 갱신

```
project      안의 내용을 환경 B C:\snatcher 에 복사해 덮어쓴다
snatcher_tool 안의 내용을 C:\snatcher\snatcher_tool 에 덮어쓴다
삭제 동기화는 하지 않는다
```

## 바로 할 수 있는 것 — GFX 게이트 판정

`lua/GFX/0.4.2-gate-window.lua` 를 올리고 **부팅 → 타이틀 → 헌정** 구간을 지난다.
장면을 맞출 필요는 없고, 무장이 걸리면 콘솔에 한 줄 찍힌다.

```
★f____  게이트 무장 -- 위치 $03:$5A · 호출자 $54E4 · 섹터 100
```

요약문이 판정을 찍는다.

```
무장 후 첫 덩어리     -> 타일 지문을 완전히 버릴 수 있다.  제일 좋은 결과
항상 n 번째           -> 덩어리 세서 n 번째에 주입
순번이 흔들림         -> 무장 + 지문 병용
무장이 안 걸림        -> 위치값이 주행마다 다르다.  다시 봐야 한다
```

## 번역

음성탭에 `ADPCM_004889_F800_0E` (7.92 초) 한 건이 `todo` 로 새로 보인다.
소리 파일도 같이 실었으면 바로 들린다 — 아니면 `1_음성_wav_뽑기.cmd` 를 한 번 돌린다.

## 환경 A로 돌아올 때

`tools/sync_from_office_20260908.py` 와 같은 방식으로 **직전 반대방향 묶음을
대조군** 삼아 병합한다. mtime 으로 가르지 말 것. 그리고 **묶음 뿌리에 낱개로
놓인 파일**을 따로 볼 것 — 09-08 에 그것 때문에 더 새 문서를 놓칠 뻔했다.
