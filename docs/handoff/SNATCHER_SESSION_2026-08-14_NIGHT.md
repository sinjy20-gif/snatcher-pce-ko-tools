# 2026-08-14 야간 세션 인계 — 이어서 할 것

짧게 쓴다.  상세 근거는 `SNATCHER_DYNAMIC_LOADING_UI_SPLIT_ANALYSIS_2026-08-14.md`.

---

## 지금 딱 멈춘 지점

**선택적 선적재 측정이 아직 안 끝났다.**  두 판 돌렸는데 둘 다 헛돌았다.

```
1판  타이틀바가 [KO 0.3.4]  = 출하본을 preresident 라는 파일명으로 기록
     그래도 건진 값: 출하 0.3.4 부팅 적재 825프레임 = 13.75초 (52청크, CD_READ 53회)
2판  cdread 0 · boot 줄 없음  = 부팅 구간을 통째로 놓침
     스크립트를 파워사이클 *뒤에* 켠 것으로 추정
```

### 내일 할 것

```
1. build\patch\0.3.4-preresident\...[KO 0.3.4-preresident].cue 열기
2. lua\PROBE_PRELOAD_STALL_0.2.0.lua  Reload -> Run
   로그 "디스크: ..." 줄에 preresident 가 있는지 확인 (프로브가 스스로 찍는다)
3. **그 다음에** 파워사이클        <- 순서가 핵심
4. "BOOT LOAD start" 뜨는지 확인.  안 뜨면 거기서 멈추고 원인부터
5. 장소 두어 군데 이동 (팩 첫 접근이 거기서 난다)
6. Stop
```

보고 싶은 값은 둘뿐이다.

```
kind=boot       35청크가 실제로 몇 초인가.  예측 약 9.5초 (14.6 -> 9.5, 5초 절감)
kind=packstall  장소 진입 첫 대사에서 몇 프레임 멈추나.  이게 감수할 만한가가 결론
```

⚠ 팩당 스톨은 **파워사이클당 한 번**뿐이다.  소프트 리셋은 AC 의 MAGIC 이 살아 있어
init 을 건너뛰므로 수요적재가 안 일어난다.

---

## 오늘 확정된 사실

```
출하 0.3.4 부팅 적재    13.75~14.63초 (실측).  기존 추정 9.8초는 낙관이었다
청크당                  약 15.9프레임 = 0.26초
2줄 레코드              고유 글리프 중앙값 16 > 상한 15.  55%가 첫 시도에서 터진다
                        슬롯을 18로 늘려도 22% 실패.  최댓값 27
                        -> {BR} 2줄은 온디맨드 글리프 공급 없이는 못 쓴다
UI pack 귀속률          164/976 (17%).  단일 101 · 공용 36 · 복수 27 · 미확정 812
                        -> UI 분할은 전 구간 수집 뒤의 일
MPR6                    6D 는 장소가 아니라 공용 뱅크.  분리해야 장소 국면이 보인다
```

---

## 오늘 바뀐 것

### 빌더 (안정판 미변경)

`build_ac_dynamic_0_1_14.py` 의 `SNATCHER_PRELOAD_PACKS` 가 3단계가 됐다.

```
all(=1)     25팩 전부   52청크    미설정 기본값은 none — 0.3.3 재현성 유지
resident    공용 4팩만  35청크
none(=0)    없음        13청크
```

`all` 로 빌드하면 출하 0.3.4 와 Track 02 바이트 동일하다(회귀 검증 통과).
테스트 디스크 `0.3.4-preall` / `0.3.4-preresident` 두 장은 Track 24 가 서로 동일하고
Track 02 헬퍼 890바이트만 다르다.

### 스튜디오 (exe 반영 완료, 23:21)

```
편입 배치     루트 상태 조각을 앞줄 밑에 꽂는다 (AuditItem.ContinuesFrom)
              직전 캡처가 CONT/BR 일 때만.  END 면 무관한 문장이라 안 붙인다
초과 표시     그리드 우선순위 맨 앞 + 입력칸 배경.  전에는 초록(신규)에 가려 안 보였고
              칸 초과만 보고 고유 글리프·용량 초과는 아예 안 봤다
VisibleNormalize  private -> public.  배치와 감사가 같은 비교 규칙을 쓴다
카탈로그      continues_from 열 추가 (옛 카탈로그는 빈 값으로 읽힘)
백업          snatcher_tool\backup\SnatcherTranslationStudio_20260814_pre_context_placement.exe
```

### 마스터 데이터

```
가우디·번호패드 179행 분리 -> master_excluded_rows.tsv (삭제 아님, 되돌릴 수 있음)
런타임 조각 131건 재배치 -> 앞줄 밑으로.  3줄 사슬도 정상
백업 20260814_232659_pre_regroup_master.tsv
```

재배치에서 잡은 실수 셋, 다시 밟지 말 것:

```
refs 덮어쓰기      런타임 state/seq 를 부모 것으로 갈아치웠다.  덧붙여야 한다
마커 중복 누적      사슬 내려가며 겹겹이 쌓였다.  마커 자체를 없앴다
임자 있는 앞줄      END 로 끝난 그룹에 3번째 줄을 붙였다.
                   -> 앞줄이 이미 다음 줄을 가졌으면 그 앞줄은 임자가 있다
```

---

## 도구

```
tools\find.py                       조각 검색 0.19초.  전체 문장 + 앞줄 기존 번역
tools\rebuild_runtime_context.py    문맥 복원 (695/816 = 85%)
tools\regroup_runtime_fragments.py  마스터 재배치 (1회성, 완료)
tools\check_master.py               빌드 차단 요소 사전 검사.  3단계 안 가고 몇 초
tools\pack_report.py                MPR6 통계(--packs) + UI 귀속률(--ui)
lua\PROBE_PRELOAD_STALL_0.2.0.lua   부팅/스톨.  BUILD 한 줄로 A/B
lua\PROBE_ADPCM_BUDGET_0.2.0.lua    음성 전/중/후 프레임 예산.  아직 안 돌림
```

---

## 미해결로 남긴 것

```
Ｄカードだ。          앞줄 인스턴스가 마스터에 없다.  9곳 전부 임자 있음.
                     소유자가 다음 수집 때 직접 편입하기로 함
앞줄 후보 여러 개 12건  す。 는 후보 15개.  자동 배정 안 함
원시 로그 4개뿐       UI 로그(runtime_ui_strings_raw.tsv)는 보관 대상에 없다.
                     세션 구분이 안 돼 UI 귀속 작업 때 걸린다
```

---

## 안 건드린 것

`build\patch\0.3.4` · `ac_0.1.14` · `ac_0.1.11-source` · `0.2.26` 등 안정판 전부.
마스터는 매번 백업 + 무변경 왕복 바이트 동일 확인 후 작업했다.
