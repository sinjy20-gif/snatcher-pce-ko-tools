# 환경 B에서 여기서부터 — 2026-09-08 CD-DA 재무장

## 먼저 읽을 것 (순서대로)

```
1. project/docs/handoff/SNATCHER_2026-09-07_NIGHT_HANDOFF.md   ★분석·빌드 이력 정본
2. project/docs/handoff/SNATCHER_CDDA_REARM_2026-09-07.md      Mednafen 계측법 · 환경 함정
```

**미해결이다.** 트랙 20 나레이션 자막이 통째로 두 번 재생된다.
자막 목록이 떨어지면 철거하는데 트랙은 4.2 초 더 남아 있고, 관문
`$F898`(`LDA $263C / ORA $2638 / BNE`)은 **"CD-DA 가 켜져 있나"** 라는
*상태*를 묻는다.  그래서 다음 프레임에 그대로 재무장한다.

rc4~rc9 는 전부 실패했다 — 되풀이하지 말 것.  실패 이력은 정본 §2 와
`FAILED_RECORDS/` 에 있다.

## 디스크는 안 실었다

환경 B에 있는 **어제 RC3 묶음의 디스크를 그대로 쓴다.**
오늘 구운 것들은 CUE·Track 24 해시가 RC3 과 전부 같다 — BIOS 만 다르다.

```
BIOS/  안의 .pce 를 게임 폴더에 넣고 Syscard3.pce 로 **복사**해서 쓴다
⚠ RetroArch 는 system_directory 의 Syscard3.pce 만 읽는다.
  이름을 안 바꾸면 예전 빌드가 조용히 돈다 -- 실제로 반나절 날렸다.
```

| 버전 | SHA256 | 비고 |
|---|---|---|
| 0.6.0-rc3-restored | `352A600040B34C1E…` | ★지금 쓸 빌드.  낮 rc3 와 바이트 동일 |
| 0.6.0-rc10-cdda-once | `F051DEC798C2294C…` | 시험 1 -- 게임 RAM $5E1F 를 써서 실패 |
| 0.6.0-rc11-cdda-ac-latch | `63AF14ED2ABAEC26…` | 시험 2 -- AC 걸쇠 |
| 0.6.0-rc12-cdda-ac-latch-init | `E0E14D6DB3C6D3EB…` | ★현재 후보 -- AC $1F1EE7 / $A5 · 초기화 추가 |

## 작업 폴더 갱신

- `project` 안의 내용을 환경 B `C:\snatcher` 에 복사해 덮어쓴다.
- `snatcher_tool` 안의 내용을 `C:\snatcher\snatcher_tool` 에 복사해 덮어쓴다.
- 삭제 동기화는 하지 않는다.

## 다음 할 일

0. ✅ **rc12 합격** (2026-09-07 밤, RetroArch 실기).

   ```
   판정 환경   RetroArch / Beetle PCE SuperGrafx   ← 실제 출하 환경
   ① 트랙 17 오프닝 자막      정상
   ② 트랙 20                  정상 (두 번 재생 안 함)
   ③ 트랙 20 뒤 ADPCM 음성    정상   ★0.6.1 이 죽었던 자리
   ```

   ⚠ 밤중에 "실패" 로 한 번 잘못 적었다가 되돌린 항목이다.  위 세 줄이 최종이다.

   rc4~rc11 은 전부 실패였다.  그 이력은 정본 §2 와 `FAILED_RECORDS/` 에 있다 --
   같은 방향으로 다시 만들지 말 것.

1. `BIOS/Syscard3_galmuri_0.6.0-rc12-cdda-ac-latch-init.pce` 를 게임 폴더에 넣고
   `Syscard3.pce` 로 **복사**해서 쓴다.
   절차서: `BIOS/0.6.0-rc12-cdda-ac-latch-init__TEST_IN_MESEN.txt`.

2. **판정 순서를 지킬 것 — ①이 먼저다.**

   ```
   ① 오프닝 트랙 17 첫 자막(36.091초)이 정상으로 뜨나
   ② 트랙 20:  CLEAR -> ARM 1 -> MARK_DONE 1 -> STOPPED 1 -> SUPPRESS 1 이상
               SUPPRESS 뒤 두 번째 ARM 0
   ③ 그 뒤 첫 ADPCM 이 살아 있나   ← 0.6.1 이 여기서 죽었다
   ```

   실패한 판들이 ①에서 죽었다.  거기서 막히면 트랙 20 까지 갈 필요가 없다.

3. **메센을 회귀 시험대로 쓴다** (400 FPS 로 1~2 분이면 "자막이 여전히 나오나" 가
   확인된다).  다만 메센에서 재무장이 안 보이는 것은 고쳐졌다는 뜻이 아니다 --
   재무장 뒤 트랙이 4.15 초 만에 끝나는데 두 번째 묶음의 첫 자막은 12.9 초
   뒤라서 화면에 못 닿는 것뿐이다.  **최종 판정은 RetroArch / Mednafen 에서.**

4. 새로 구울 일이 생기면 빌드 순서는 계측 문서 §3 을 따를 것 —
   `patch_bios_cdda_scheduler.py` 는 **돌리지 말 것.**

## 스튜디오

CD-DA 타임라인을 고쳤다 (2026-09-07).

- 재생이 확대 창을 끌고 다니지 않는다.  `따라가기` 스위치로 분리, 기본 꺼짐.
- 지금 말하는 자막은 **표시만** 한다 (♪ 딱지 + 밑줄).
- 가로 스크롤 막대 추가.  휠 = 좌우, Ctrl+휠 = 배율.
- 블록을 잡을 때 창이 튀던 것, 편집할 때마다 창이 되돌아가던 것도 같이 고쳤다.

## 추가 실주행 확인 — 2026-09-08

소유자가 현재 0.6.2 계열을 챕터 2 초반까지 직접 진행했고, 그 범위에서
CD-DA와 ADPCM 자막 전환 및 재생은 정상적으로 동작했다. 트랙 20 이후 ADPCM이
죽던 과거 회귀도 이 주행 범위에서는 다시 나타나지 않았다.

별도로 조사 중인 배경 밀림/흔들림은 남아 있으나, 이 현상과 CD-DA·ADPCM의
기능 정상 여부는 분리해서 관리한다.
